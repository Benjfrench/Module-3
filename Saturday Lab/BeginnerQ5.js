// Question 5

function getGreeting(name) {
    return "Hello " + name + "!";
  }

  // Answer
  // a
  const getGreetings_function = function (name) {
    return "Hello " + name + "!";
  };

  // b
  const getGreetings_arrow = (name) => {
    return "Hello " + name + "!";
  };

